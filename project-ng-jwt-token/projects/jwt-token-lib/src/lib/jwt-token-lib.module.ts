import { NgModule } from '@angular/core';
import { JwtTokenLibComponent } from './jwt-token-lib.component';

@NgModule({
  declarations: [JwtTokenLibComponent],
  imports: [
  ],
  exports: [JwtTokenLibComponent]
})
export class JwtTokenLibModule { }
